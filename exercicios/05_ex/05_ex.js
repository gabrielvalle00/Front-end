/*
Construir um programa que leia três valores
numéricos inteiros, representados pelas variáveis A, B e C em seguida realiza soma do
resultado do quadrado de cada uma das variáveis, para calcular o quadrado
devemos multiplicar um número por ele mesmo.

*/

var a = 29;
var b = 12;
var c = 8;

var soma = (a*a) + (b*b) + (c*c);

console.log(soma);
