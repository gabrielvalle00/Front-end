/*
E um bloco de codigo que se repete ate que uma condição seja satisfeita.
Isso evita repetição desnecessaria do nosso codigo.
Algumas das estruturas são o for e o whilhe;
As suas ssintaxes são diferentes, mas as duas chegam aos...
*/
console.log('-----------01-------------');

let p = 0;
while (p <= 5) {
    console.log(`Repetindo ${p}`);
    //p = p + 1 
    p += 1
}